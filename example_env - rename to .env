#### Rename file to .env and fill it out according to your own setup before running docker-compose.

### Docker-compose Variables
COMPOSE_PROJECT_NAME=mediaServer
### Time Zone
CUSTOM_TIME_ZONE=Europe/London

### Plex variables
## Required to access the Plex Media Server
PLEX_MEDIA_SERVER_PORT=32400

# Plex Advertise IP
PLEX_HOST_SCHEME=http://
PLEX_HOST_IP=some.ip.goes.here

# Only needed in local network.
PLEX_DLNA_SERVER_PORT=1900
PLEX_COMPANION_PORT=3005
PLEX_DLNA_SERVER_ACCESS_PORT=32469
PLEX_FOR_ROKU_PORT=8324
GDM_NETWORK_PORT1=32410
GDM_NETWORK_PORT2=32412
GDM_NETWORK_PORT3=32413
GDM_NETWORK_PORT4=32414

## Plex volumes
PLEX_DATABASE_LOC=./plex/database
PLEX_TRANSCODE_LOC=./plex/transcode
PLEX_MEDIA_LOC=./plex/media

## Media Volumes - MUST CHANGE
TVSHOWS_LOC=/use/your/own/locations/forShows
MOVIES_LOC=/use/your/own/locations/forMovies
DOWNLOADS_LOC=/use/your/own/locations/forDownloads

### Radarr variables
# Radarr config path
RADARR_CONFIG_PATH=./radarr
# Radarr access port
RADARR_ACCESS_PORT=7878

### Sonarr variables
# Sonarr config path
SONARR_CONFIG_PATH=./sonarr
# Sonar access port
SONARR_ACCESS_PORT=8989

### Organizr variables
## Organizr Settings
ORGANIZR_FPM=false #Used to enable php to use the socket rather than TCP, often increases performance. Valid values are(comma separated) true, false
ORGANIZR_MAIN_BRANCH=v2-master #v2-master, master, v2-develop, develop, dev
# Organizr config path
ORGANIZR_CONFIG=./organizr
# Organizr access port
ORGANIZR_PORT=80

### Bazarr variables
# Bazarr config path
BAZARR_CONFIG_PATH=./bazarr
# Bazarr access port
BAZARR_ACCESS_PORT=6767

### Prowlarr variables
## Prowlarr config path
PROWLARR_CONFIG=./prowlarr
# Prowlarr access port
PROWLARR_PORT=9696

### qBitTorrent variables
# qBitTorrent Port
QBITTORRENT_PORT=8080
QBITTORRENT_CONFIG=./qbittorrent

### filebot variables
FILEBOT_CONFIG=./filebot
FILEBOT_PORT=7676

## results in file names like "The Expanse - S02E07 - The Seventh Man - HD.mkv"
FILEBOT_FORMAT={plex} - {hd}
## prefer titles from the last 30 days but fall back to any title if there are none recent enough
FILEBOT_FILTER=any{ age < 30 }{ true } || !model.any{ it.age < 30 && it.age != null }
## duplicate will fall back to copy if hardlink fails. set to "hardlink" to confirm if hardlinks are working
FILEBOT_ACTION=hardlink #duplicate
## filebot will intelligently decide whether or not to replace an existing copy
FILEBOT_CONFLICT=auto
## defaults to TheTVDB but TheTVDB has been having issues for most of 2020
FILEBOT_SERIES_DB=TheMovieDB::TV
FILEBOT_ANIME_DB=AniDB
FILEBOT_MOVIE_DB=TheMovieDB
FILEBOT_MUSIC_DB=ID3
## not currently functional
OPEN_SUB_USER=[REPLACEME]
## not currently functional 
OPEN_SUB_PASS=[REPLACEME]

### Overseerr variables
## Overseerr config path
OVERSEERR_CONFIG=./overseerr
# Overseerr access port
OVERSEERR_PORT=5050

### Apache variables
## This you can change if port 80 on the machine is already in use.
APACHE_PORT=80